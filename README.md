# 航站楼安检客流资料服务

仓库中的匿名样例包含安检区队列观测、迟到更正、通道能力和员工岗位资质，不采集单个旅客身份。观测同时记录现场发生时间与接收时间，用来区分历史当时可知的信息和事后修正。

Node.js 20 以上版本可运行 `npm test` 与 `npm start`，进程探针是 `GET /health`。容器入口为 `docker compose up --build`，宿主机端口可由 `APP_PORT` 配置。生产传感器地址、人员身份和排班凭据不得提交。
