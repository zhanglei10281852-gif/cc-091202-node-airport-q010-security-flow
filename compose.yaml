services:
  app:
    build: .
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      PORT: 3000
      DATA_DIR: /data
    volumes:
      - flow-data:/data
    restart: unless-stopped

volumes:
  flow-data:
